# App metadata (exposed to the client — never put secrets in VITE_* vars)
VITE_APP_NAME="Job Application Tracker"
VITE_APP_VERSION="0.0.0"

# Local storage namespace prefix for persisted slices
VITE_STORAGE_PREFIX="jat"
